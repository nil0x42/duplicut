Features:
  * Add file backup mechanism (and --no-backup option).
  * Implement multi threading and work queue for chunk cleanup tasks.
  * Add a fast way to remove duplicates on small files.

Unittest:
  * Add a result comparator script.
  * Test wordlists with nullbytes.

Compilation:
  * Add link time optimisations.
